import Head from 'next/head';

const Header = () => {
	return (
		<Head>
			<title>Dine Restaurant Website</title>
			<meta name='description' content='Dine Restaurant Website' />
			<link rel='icon' href='/favicon.ico' />
		</Head>
	);
};

export default Header;
